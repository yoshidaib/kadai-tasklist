<h1>タスク新規作成のページ</h1>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content, placeholder: 'タスクを入力'; %>
  
  <%= f.submit '作成' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>